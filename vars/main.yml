---
# Role variables

# Required packages

bootstrap_packages:
  - libselinux-python
  - >-
    {{ (bootstrap_distribution_version is version("8", "<"))
       | ternary("python-virtualenv", "python3-virtualenv") }}
